# claude-tools

~40 个 Claude Code Tools + agentboard 仪表盘，统一管理、统一恢复。

## 结构

每个子目录 = 一个 tool，包含 `manifest.json` + Agent 可发现的所有资源。

## 安装

```bash
git clone https://github.com/wampeeHuang/claude-tools.git ~/.claude/tools/
```
