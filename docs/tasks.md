# Agentboard 任务与过程记录

用途：agentboard 开发过程文件。跨会话追踪任务、决策、进度。覆盖式更新，不追加。

## 任务清单

| # | 状态 | 任务 |
|---|------|------|
| 33 | 待做 | live 卡重构（设计基准 card-data-contract.md；card-v2.html 原型已删 2026-08-26） |
| 34 | 已完成 | 左侧导航 + 子页前端原型（`web/_proto/dashboard-leftnav.html`） |
| 35 | 待做 | 自启动剥离 registry：删自启动 tab + 工具架卡片补自启徽标 |

## 活动线程

**导航重构原型** `web/_proto/dashboard-leftnav.html`（file:// mock 数据）~~已归档 2026-08-26：原型删除，6 页导航落地~~

- 导航 5 项：工具架 / 我的网站 / 经验日志 / 说明书 / 系统规范（外链 /registry）
- 每页顶部 128px 深军绿 `#2E5038` 英雄栏通栏，标题 32/400 系统字体，副标题 16/300 等宽
- 侧栏网格 128/32/8 多层线，logo 块凑 128px 高使分隔线落网格线
- 底部 foot：`双平面 一份真相`，块高 64px、字号 16px（等宽）
- 计数全进左侧导航栏：工具架10 / 我的网站7 / 经验日志6 / 系统规范2，右侧内容区不出现数字
- 工具架筛选区 3 行（去按钮，文本式）：
  - 第 1 行 64px = 一级分类（4 维度，单选，默认功能分类 active）
  - 第 2 行 64px = 当前维度的二级选项（点维度切换内容，永不空；点选项筛选、选中绿色+✕）
  - 第 3 行 = 搜索框 + 搜索按钮（placeholder 提示输入工具名/功能/描述）
- 状态统计条（10全部·5运行中·3未启动·1公开站）已移除 → 待独立可视化面板呈现

## 已定决策

- **注册表 → 系统规范**：`/registry` 页含 3 tab（自启动 / 设计规范 / 工程规范）。自启动本质是工具属性，应归工具架卡片层，registry 剥离后只剩 2 份规范，改名"系统规范"。两份规范（design-spec/repo-spec）是 agentboard 自身的设计+工程约定（非仅工具），静态 md 落盘 `docs/`。
- **命名已落地**：原型 nav + 真 dashboard header 链接（`index.html:40`）已改"系统规范"，title 改"设计规范 + 工程规范"。
- **自启动机制现状**：manifest `autoStart`（卡片编辑表单 index.html:283）= 声明层，从不写 Startup 文件夹；registry 自启动 tab = 扫描 Windows Startup + `~/.agentboard/` bat/vbs/ps1（落地层）。两层互不写入，会漂移。→ 归并方案见任务 #35。
